# permcheck

API safety layer for TypeScript backends: request validation, structured responses, rate limiting, idempotency, and API keys.

## Commands
bun test — Run tests
bun run typecheck — tsc --noEmit
bun run lint — ESLint
bun run check — lint + format:check + typecheck + test
bun run build — Build to dist/

## Key API
createPermcheck({ apiKeys, rateLimit, idempotency, client }) -> Permcheck
permcheck.validate(schemas, request) — Validate body/query/params/headers with Zod schemas
permcheck.ok(data) — { success: true, data }
permcheck.fail(msg, code?, details?) — { success: false, error: { message, code, details } }
permcheck.paginated(data, total, page, limit) — paginated response with pages calc
permcheck.problem(detail) — RFC 9457 problem details response

## Sub-modules (deep imports)
permcheck/validate — validateRequest, validate
permcheck/respond — ok, fail, paginated, problem
permcheck/idempotency — idempotency(), InMemoryStore, types
permcheck/rate-limit — rateLimit(), InMemoryRateLimitStore, keyByApiKey
permcheck/api-keys — createApiKeyValidator (in-memory)
permcheck/errors — PermcheckError, ValidationError, AuthenticationError, RateLimitError, IdempotencyError, isPermcheckError
permcheck/stores/redis — fromIORedis, RedisIdempotencyStore, RedisRateLimitStore
permcheck/stores/redis-api-keys — RedisApiKeyStore
permcheck/stores/postgres — PostgresIdempotencyStore, PostgresRateLimitStore
permcheck/stores/postgres-api-keys — PostgresApiKeyStore (with ensureTable)
permcheck/adapters/hono — createRateLimiter, requireIdempotencyKey, permcheckMiddleware

## Hono Adapter
createRateLimiter({ permcheck, keyPrefix, getKey? }) — Rate limit by custom key (window/max from permcheck config)
requireIdempotencyKey({ permcheck, keyHeader? }) — Validates Idempotency-Key header, caches responses
permcheckMiddleware(permcheck, { auth?, requiredScopes?, rateLimit?, idempotency?, excludePaths? }) — All-in-one middleware

## Patterns
Framework-agnostic design — plain functions, not tied to any HTTP library.
Zod schemas for all validation. In-memory stores default; Redis/Postgres stores available.
Remote client via `client` option re-routes rate-limit, idempotency, and API key validation with local fallback on NetworkError.
All errors extend PermcheckError with code and status properties.
